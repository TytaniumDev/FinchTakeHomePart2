import 'package:flutter/animation.dart';

const kVibeTransitionDuration = Duration(milliseconds: 300);

/// Playful overshoot curve for bouncy elements (speech bubble, etc.).
const kBouncyCurve = Curves.easeInOutBack;

/// Standard smooth curve for color changes, crossfades, and layout lerps.
const kStandardCurve = Curves.easeInOut;
